{
  "buildCommand": "npm install --no-audit --no-fund && npm run build && cd demo && npm run build",
  "outputDirectory": "demo/.next",
  "framework": "nextjs",
  "installCommand": "npm install --no-audit --no-fund && npm run build && cd demo && npm install --no-audit --no-fund",
  "devCommand": "cd demo && npm run dev",
  "functions": {
    "demo/app/**/*.tsx": {
      "maxDuration": 30
    }
  }
} 
