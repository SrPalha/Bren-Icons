name: Publish to NPM

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        registry-url: 'https://registry.npmjs.org'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Build project
      run: npm run build
    
    - name: Publish to NPM
      run: npm publish
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
    
    - name: Create GitHub Release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: Release ${{ github.ref }}
        body: |
          ## 🚀 Nova versão publicada!
          
          ### 📦 NPM Package
          - **Nome**: bren-icons
          - **Versão**: ${{ github.ref_name }}
          - **Link**: https://www.npmjs.com/package/bren-icons
          
          ### 🔄 Instalação
          ```bash
          npm install bren-icons@${{ github.ref_name }}
          ```
          
          ### 📋 Changelog
          Verifique o arquivo [CHANGELOG.md](CHANGELOG.md) para detalhes das mudanças.
        draft: false
        prerelease: false
